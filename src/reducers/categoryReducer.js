const categoryReducer = (state= {
    categories: [],
    loading: false
}, action) => {
        //let idx;
        switch(action.type) {

        default:
            return state;
        }
    }


export default categoryReducer