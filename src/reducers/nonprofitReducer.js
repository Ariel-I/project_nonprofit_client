const nonprofitReducer = (state = {
    nonprofits: [],
    loading: false
}, action) => {
    //let idx;
    switch(action.type) {

        default:
            return state
    }
}

export default nonprofitReducer